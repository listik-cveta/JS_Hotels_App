<h1>Reservation:</h1>


<h2>Hotel: <%= Hotel.find_by_id(@reservation.hotel_id).name %></h2>
<h3>Check In Date: <%= @reservation.check_in.strftime("%A, %B %d, %Y") %></h3>
<h3>Price per night: $<%= Hotel.find_by_id(@reservation.hotel_id).cost %></h3>
<h3>Number of nights: <%= @reservation.num_nights %></h3>
<h3>Number of guests: <%= @reservation.num_guests %></h3>

<h4>Total Cost: $<%= @reservation.num_nights * Hotel.find_by_id(@reservation.hotel_id).cost %></h4>
<h4><%= link_to "Edit Reservation", edit_reservation_path(@reservation) %></h4>
<h4><%= link_to "Delete Reservation", reservation_path(@reservation), method: :delete, data: { confirm: 'Are you sure you want to delete this reservation?' } %></h4>
<h4><%= link_to 'View All Reservations',user_path(current_user) %></h4>