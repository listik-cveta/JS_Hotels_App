<%= form_for @reservation do |f| %>

  <%= f.label "Select Hotel" %>
  <%= select_tag :hotel_id, options_from_collection_for_select(Hotel.all, :id, :name, @reservation.hotel_id) %><br><br>

  <%= f.label "Check in date" %>
  <%= f.date_field :check_in %><br><br>

  <%= f.label "Number of nights" %>
  <%= f.number_field :num_nights %><br><br>

  <%= f.label "Number of guests" %>
  <%= f.number_field :num_guests %><br><br>

  <%= f.fields_for :guests do |guest| %>
    <%= guest.label :name %>
    <%= guest.text_field :name %>

    <%= guest.label :email %>
    <%= guest.text_field :email %>
  <% end %>

  <%= f.submit %> 

<% end %>

